project:
  machine_name: jumpstart-demo
  prefix: BLT
  human_name: 'BLTed 8'
  profile:
    name: jumpstart-demo
  local:
    protocol: http
    hostname: 'jumpstartdemo.loc'
git:
  default_branch: master
  remotes: {  }
deploy:
  tag_source: true
drush:
  aliases:
    remote: '${project.machine_name}.test'
    local: self
    ci: self
  default_alias: '${drush.aliases.local}'
